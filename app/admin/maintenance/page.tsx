'use client';

import { SectionHeader, Tabs } from '@/components/ui';
import ManualTab from './ManualTab';
import PipelineTab from './PipelineTab';
import AgenticTab from './AgenticTab';
import MonitoringTab from './MonitoringTab';
import DashboardTab from './DashboardTab';
import ReportTab from './ReportTab';
import GovernanceTab from './GovernanceTab';
import UserStoryTab from './UserStoryTab';
import TestingTab from './TestingTab';
import LogTrackingTab from './LogTrackingTab';
import styles from './AdminMaintenance.module.css';

// Module 27 on the Operational Portal 10-tab standard. The most
// significant fix this session: isMaintenanceMode() had zero real
// callers anywhere -- the toggle wrote a real settings row but had no
// actual effect on the public site. Fixed via a new public status
// route + real middleware.ts enforcement (see GovernanceTab for the
// full explanation). Pipeline/Agentic perform a real live self-test
// against the public homepage, not a simulated check.
export default function AdminMaintenancePage() {
  return (
    <div className={styles.page}>
      <SectionHeader label="Operations" title="Maintenance Mode" subtitle="Control site-wide maintenance mode with custom messages, via Manual, Pipeline (real enforcement self-test), or Agentic (local LLM) execution modes." />
      <Tabs
        tabs={[
          { id: 'manual', label: 'Manual', content: <ManualTab /> },
          { id: 'pipeline', label: 'Pipeline', content: <PipelineTab /> },
          { id: 'agentic', label: 'Agentic', content: <AgenticTab /> },
          { id: 'monitoring', label: 'Monitoring', content: <MonitoringTab /> },
          { id: 'dashboard', label: 'Dashboard', content: <DashboardTab /> },
          { id: 'report', label: 'Report', content: <ReportTab /> },
          { id: 'governance', label: 'Governance', content: <GovernanceTab /> },
          { id: 'user-story', label: 'User Story', content: <UserStoryTab /> },
          { id: 'testing', label: 'Testing', content: <TestingTab /> },
          { id: 'log-tracking', label: 'Log & Tracking', content: <LogTrackingTab /> },
        ]}
      />
    </div>
  );
}
